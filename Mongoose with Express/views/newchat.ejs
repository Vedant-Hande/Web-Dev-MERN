<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>new Chat</title>
    <link rel="stylesheet" href="/Mongoose with Express/public/newchat.css" />
    <style>
      body {
        background: linear-gradient(135deg, #74ebd5 0%, #acb6e5 100%);
        font-family: "Segoe UI", Arial, sans-serif;
        display: flex;
        justify-content: center;
        align-items: center;
        height: 100vh;
        margin: 0;
      }

      .chat-form {
        background: #fff;
        padding: 36px 32px 28px 32px;
        border-radius: 18px;
        box-shadow: 0 8px 32px rgba(44, 62, 80, 0.15);
        min-width: 340px;
        max-width: 400px;
        width: 100%;
        border: 1.5px solid #e0e0e0;
      }

      .chat-form h1 {
        margin-top: 0;
        margin-bottom: 22px;
        color: #2980b9;
        font-size: 2em;
        text-align: center;
        letter-spacing: 1px;
      }

      .chat-form label {
        display: block;
        margin-bottom: 7px;
        color: #34495e;
        font-weight: 600;
        letter-spacing: 0.5px;
      }

      .chat-form input,
      .chat-form textarea {
        width: 100%;
        padding: 10px 12px;
        margin-bottom: 18px;
        border: 1.5px solid #b2bec3;
        border-radius: 8px;
        font-size: 1em;
        background: #f8fafc;
        transition: border 0.2s, box-shadow 0.2s;
        box-sizing: border-box;
      }

      .chat-form input:focus,
      .chat-form textarea:focus {
        border: 1.5px solid #2980b9;
        outline: none;
        box-shadow: 0 0 0 2px #b2e0fb44;
      }

      .chat-form button {
        width: 100%;
        padding: 12px 0;
        background: linear-gradient(90deg, #2980b9 60%, #6dd5fa 100%);
        color: #fff;
        border: none;
        border-radius: 8px;
        font-size: 1.15em;
        font-weight: 700;
        cursor: pointer;
        transition: background 0.2s, box-shadow 0.2s;
        box-shadow: 0 2px 8px #b2e0fb33;
        letter-spacing: 0.5px;
      }

      .chat-form button:hover {
        background: linear-gradient(90deg, #2573a6 60%, #4fc3f7 100%);
        box-shadow: 0 4px 16px #b2e0fb55;
      }
    </style>
  </head>
  <body>
    <form class="chat-form" method="" action="">
      <h1>Start a new chat</h1>
      <label for="from">Sender:</label>
      <input type="text" id="from" name="from" required />

      <label for="to">Receiver:</label>
      <input type="text" id="to" name="to" required />

      <label for="msg">Message:</label>
      <textarea id="msg" name="msg" rows="4" required></textarea>

      <button type="submit">Send</button>
    </form>
  </body>
</html>
